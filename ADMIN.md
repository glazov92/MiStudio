# ✏️ Админка сайта «Mi Studio» — простой гайд

Этот гайд — как зайти в админку, что можно менять и как не потерять изменения.
Ничего технического знать не нужно.

---

## 1. 🚪 Как зайти в админку

Админка «включается» прямо на сайте — отдельной страницы входа нет.
Нужно добавить к адресу сайта **пароль из двух частей**:

```
?edit=FZF6MGgNAq4mU653tHkPvCSP
```

**Как сделать (по шагам):**

1. Открой сайт в браузере (Chrome или Edge).
2. Нажми один раз в **адресную строку** (там, где написан адрес сайта).
3. В самом **конце** адреса допиши `?edit=FZF6MGgNAq4mU653tHkPvCSP` и нажми `Enter`.

> **Слэш в конце адреса не важен.** Если в адресной строке после адреса стоит
> `/` (например, `https://твой-сайт.ru/`) — просто дописывай `?edit=…` после
> него, ничего не стирая. И `file:///…/index.html?edit=…`, и
> `file:///…/index.html/?edit=…` работают одинаково.

Пример. Сайт пока открыт с компьютера, адрес выглядит так:

```
file:///C:/Users/user/Desktop/MiStudio/index.html?edit=FZF6MGgNAq4mU653tHkPvCSP
```

А когда сайт будет на хостинге, будет так:

```
https://твой-сайт.ru/?edit=FZF6MGgNAq4mU653tHkPvCSP
```

4. Внизу страницы появится **золотистая панель «✏️ Режим редактирования»**. Всё — ты в админке.
5. Выйти из админки: кнопка **«Выйти»** на панели.

### ⚠️ Три частые ошибки

| Ошибка | Что делать |
| --- | --- |
| Вставил ключ без `?edit=` (просто ключ в конце адреса) | Тоже работает — но привычнее и надёжнее с `?edit=` перед ключом |
| Перепутал ключи | Для входа нужен **админский** ключ: `FZF6MGgNAq4mU653tHkPvCSP`. Серверный ключ (в адрес вставлять не надо) — свой, задан при деплое |
| Панель не появилась, хотя адрес верный | Обнови страницу с очисткой: нажми `Ctrl + F5`. Если открывал сайт раньше — браузер мог запомнить старую версию |

> Если в адресе что-то не так (забыл `edit=` или ключ неверный) — админка сама
> подскажет, что исправить.

---

## 2. 🖱️ Что и как можно менять

**Текст.** Наведи на текст (заголовок, описание, цена) → кликни → откроется окно
редактирования → поменяй текст → нажми **«Сохранить»**. Текст на сайте заменится.

**Картинку.** Кликни по картинке → выбери файл с компьютера → картинка сама
уменьшится до нужного размера → **«Сохранить»**.

**Кнопки на панели (внизу):**
- 🎯 **Акции** — добавить/убрать акцию, поменять срок действия.
- 🔧 **Услуги** — добавить услугу, изменить цены, мастеров и состав.
- 📁 **Портфолио** — добавить/убрать фотографии работ.
- 🔗 **Ссылки** — телефоны, адрес, соцсети.
- 💬 **Тексты** и 🖼️ **Изображения** — список всех текстов/картинок страницы
  (удобно, если нужно найти конкретный).

---

## 3. 💾 Сохранение

- Правки копятся сами. Чтобы они попали на сайт **для всех посетителей** —
  нажми **«💾 Сохранить»** на панели. Появится надпись
  «опубликовано на сервере ✓».
- Если сайт ещё не на хостинге или нет интернета — будет надпись
  «Сохранено локально». Правки при этом сохранятся в этом браузере, но
  у других людей их не будет, пока сайт не на хостинге.

---

## 4. ↩️ Как отменить

- **Один элемент.** В окне редактирования текста/картинки есть кнопка
  **«Сбросить к оригиналу»** — вернёт то, что было до правки.
- **Версию целиком.** Кнопка **🕘 История** на панели показывает сохранённые
  версии с датами. Нажми **«↩️ Откатить»** у нужной даты — сайт вернётся к
  состоянию того дня. Перед откатом текущее состояние тоже сохраняется,
  так что откат всегда можно отменить.
- ⛔ **Осторожно: «🗑️ Сбросить»** удаляет **все** правки разом
  (тексты, картинки, акции, услуги, портфолио). Спрашивает подтверждение,
  но вернуть обратно будет нечего — перед этим лучше сделать экспорт (см. ниже).

---

## 5. 📦 Резервная копия (на всякий случай)

- **📤 Экспорт** — скачивается файл с копией всех правок. Храни его у себя
  (например, на флешке или в облаке). Раз в неделю — экспорт.
- **📥 Импорт** — загрузить правки из такого файла (например, на другом
  компьютере).
- **🧹 Чистка** — удаляет старые загруженные картинки, которые больше
  нигде не используются (ни на сайте, ни в истории версий). Картинки,
  нужные сайту или любому откату, удалить невозможно — панель их защищает.

---

## 6. 🖼️ Про картинки

- Загруженная с компьютера картинка **сохраняется файлом на хостинге**
  (папка `img/u/`), автоматически сжимается до 1600 px и конвертируется
  в лёгкий формат. Вставлять ссылки вручную больше не обязательно.
- Одна и та же картинка, загруженная дважды, не занимает место дважды.
- Если сервер временно недоступен — картинка сохранится по-старому
  (внутри правок), ничего не потеряется.

---

## 7. 💾 Как правильно публиковать

1. Внеси правки — в окошках жми **«Применить»** (правка принята в работу,
   на кнопке 💾 появится красная точка).
2. Когда закончил — жми **«💾 Сохранить»** на панели: всё уедет на сайт
   разом, в ВК придёт одна сводка изменений.
Красная точка горит, пока есть непубликованное. Если закрыть страницу
без 💾 — браузер спросит «точно уйти?», а правки останутся в этом браузере.

---

## 8. 📜 История правок в ВК

Каждое сохранение автоматически отправляет краткую сводку изменений в беседу
ВК (туда же, где заявки): что изменено, что добавлено/удалено, откаты и чистка.
Это дублирует кнопку «🕘 История» — удобно, чтобы все видели активность без
входа в админку. Сообщение не приходит, если сохранить без изменений.

---

## 9. ❓ Если что-то не работает

| Ситуация | Что делать |
| --- | --- |
| Панель не появилась | Проверь адрес: точно ли в конце `?edit=FZF6MGgNAq4mU653tHkPvCSP` (с `?edit=`!). Нажми `Ctrl+F5` |
| «Сохранено локально, сервер недоступен» | Сайт ещё не на хостинге или нет интернета. Правки живут в этом браузере — они никуда не денутся, пока не очистишь его данные |
| Ничего не сохраняется | Открыта «скрытая вкладка» (инкогнито) — там админка не сохраняет. Открой обычное окно |
| Что-то сломалось | Сделай **📤 Экспорт** (сохранит файл), затем можно **🗑️ Сбросить**, а после — **📥 Импорт** из файла |

---

*Ключ админки: ссылка вида `https://studiomi.ru/?edit=FZF6MGgNAq4mU653tHkPvCSP`. Ключ проверяется сервером и нигде не хранится в коде сайта.*
*Запуск и настройка сайта на хостинге — в `DEPLOY-ADMINVPS.md`.*
