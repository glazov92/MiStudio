# MiStudio — сайт салона красоты

## СТАТУС (на 12.08.2026) — продолжение в новой сессии
**Готово (v1.0):**
- Лендинг: один index.html со всеми секциями (акции, услуги, о нас, портфолио, контакты) + единые css/style.css и js/main.js. Отдельные services.html/portfolio.html/contacts.html/404.html удалены (были осиротевшими).
- Общие компоненты рендерятся JS: хедер (sticky, бургер, дропдаун «Услуги»), футер, попапы (заявка, политика, услуга), плавающие кнопки WA/TG/звонок, sticky-CTA на мобильных.
- Услуги: 5 категорий из js/data.js; прайсы — аккордеоны (`details.price-acc`), карточки открывают попап с деталями категории (`data-open-service`).
- Карусели (стрелки + свайп), галерея с лайтбоксом, Яндекс-карта, форма заявки с полем visit_time.
- Фото: локальные в img/ (скачаны с mibeauty-studio.ru). Дизайн-токены ниже.
- Визуальный редактор контента: правки текстов и изображений + CRUD-менеджеры акций, услуг и портфолио (localStorage + PHP-синхронизация на сервере через content-sync.php) — см. раздел «Визуальный редактор».
- Развёртывание на AdminVPS (тариф Promo, виртуальный хостинг, панель ispmanager, PHP): пошаговый гайд `DEPLOY-ADMINVPS.md`. PHP-скрипт `content-sync.php` хранит правки админки в `mistudio-data/editor-store.json` (вне веб-корня) — изменения видны всем посетителям.
- Проверка: Edge headless `msedge.exe --headless --disable-gpu --dump-dom file:///...` рендерит все страницы без JS-ошибок.

**Осталось (открытые задачи):**
1. **Развернуть сайт на AdminVPS Promo** по `DEPLOY-ADMINVPS.md` (заказ хостинга, домен/DNS, загрузка файлов, SSL, заменить ключ CMS_KEY в config.js и content-sync.php).
2. **Telegram-вебхук** (`webhook/telegram-webhook.gs`): резерв — не используется (основной канал — VK-бот + email).
3. VK-бот — только задел (`CONFIG.vkBotUrl` пусто), в v1.0 не делать.

**Важно:** на этой машине НЕТ node. Проверять JS можно через Edge headless или Python. Сайт живёт в `C:\Users\user\Desktop\MiStudio\`, контент-донор — `Desktop\Mi studio 2\`, живой референс — https://mibeauty-studio.ru/.

## Визуальный редактор (?edit=FZF6MGgNAq4mU653tHkPvCSP)
- Пользовательский гайд для владельца сайта: `ADMIN.md` (как войти, править тексты/картинки, менеджеры, сохранение, экспорт/импорт, откат, как изменения попадают на хостинг).
- Включение: `?edit=КЛЮЧ` (или голый `?КЛЮЧ`). С 24.08.2026 (вечер) ключ проверяет СЕРВЕР (`content-sync2.php?action=auth`, PHP-сессия) — в браузер/config.js ключ не попадает вообще; офлайн-админка (file://) отключена осознанно. (ключ пароля — FZF6MGgNAq4mU653tHkPvCSP, при неверном ключе панель не показывается; с 16.08.2026 работает и голый `?КЛЮЧ`). Ветка: `feature/admin-cms`. С 24.08.2026 ключ в config.js ОТСУТСТВУЕТ: активация панели — через серверную проверку (`action=auth` + PHP-сессия); сохранения идут по сессии без ключа. Ключ хранится только на сервере (CMS_KEY в content-sync*.php) и в доках для доверенных.
- Защита сайта (16.08.2026): `content-sync.php` проверяет Origin (свой домен или localhost) и троттлит записи (1 POST/сек → 429). **С 19.08.2026** данные админки (`editor-store.json`, версии) хранятся в `mistudio-data/` — за пределами веб-корня (nginx отдаёт 404), папка `data/` в веб-корне закрыта Basic Auth панели (401). Полной серверной авторизации нет — это осознанно «дверь с защёлкой». Репозиторий приватный.
- Правка текста: в режиме редактирования клик по тексту → модальное окно → Сохранить / Сбросить к оригиналу. Правка картинки: клик по `<img data-editable="image">` → загрузка файла, превью, автожаттие до 1600px, сохранение в base64.
- Секции: панель внизу страницы → «Акции», «Услуги», «Портфолио» → менеджер (добавить/редактировать/удалить/перетаскивать для сортировки). После сохранения секция пере-рендерится без перезагрузки (функции `renderPromos`/`renderServicePreviews`/`renderGallery` и т.д. — пере-инициализация каруселей/лайтбокса идемпотентна через `__*Cleanup`).
- Данные хранятся в localStorage (ключи `miedit_texts`, `miedit_images`, `miedit_promos`, `miedit_services`, `miedit_portfolio`), применяются при загрузке страницы. Импорт/экспорт JSON и сброс — в панели. Ограничения: правки локальны для браузера и привязаны к `data-editable-id` (не менять эти id в HTML без причины).
- **PHP-синхронизация (v1.1):** при `EDITOR_CONFIG.serverSync.enabled=true` каждое сохранение дополнительно отправляет полный снимок правок на `content-sync.php` (POST `{key, data}`), а при загрузке страницы правки подтягиваются (GET) и применяются (`editorApplyServerData` — для карт тексты/картинки/paths/links серверные значения побеждают по ключу, локальные не затираются; для списков акции/услуги/портфолио сервер полностью побеждает). Кнопка «💾 Сохранить» показывает «опубликовано на сервере ✓» либо «сохранено локально». «🗑️ Сбросить» очищает и сервер. Ключ записи — `serverSync.key` в config.js = `CMS_KEY` в content-sync.php. Скрипт пишет в `mistudio-data/editor-store.json` (вне веб-корня, в git не идёт). Offline/без сервера (file://, старый деплой) — всё работает только на localStorage.
- **Версии правок (v1.2, «🕘 История»):** при каждом успешном POST-сохранении `content-sync.php` делает снапшот в `mistudio-data/versions/v-<Ymd>-<His>[-N].json` (последние `MAX_VERSIONS`=15, файлы до `MAX_SNAPSHOT_BYTES`=1,5 МБ; каталог вне веб-корня, nginx не отдаёт). Эндпоинты: GET `?action=versions&key=` → `{ok, versions:[{id,time}], max}`; POST `{action:'restore', key, version}` — перед откатом текущее состояние тоже сохраняется как версия (откат отменяем). `safeVersionId` валидирует id (`v-\d{8}-\d{6}(-\d+)?`), путь за пределы versions исключён. Фронт: `editorSyncVersions()`/`editorSyncRestore()` (js/storage.js), кнопка `data-ed-history` + `openHistoryModal()` (js/editor.js), стили `.ed-btn--restore` (css/editor.css). Бэкап-гайд (3 уровня: История → Экспорт JSON → модуль ispmanager) — раздел 9 в DEPLOY-ADMINVPS.md, ADMIN.md раздел 11.
- Изображения: редактируются ВСЕ — hero, «О нас», карточки услуг, слайды каруселей, фото мастеров в попапах, галерея. Разметка динамических: атрибут `data-editor-img="<ключ>"`, ключи `svc_<id>_img_<i>` / `svc_<id>_master_<k>` / `gallery_<id>` (см. `svcImageKey`/`galleryImageKey` в js/main.js). Полный список — кнопка «🖼️ Изображения» в панели (работает и когда открыт попап: клик по любому `<img>` в режиме редактирования открывает замену). Оверрайды применяются в `getServices()`/`getPortfolioItems()`.
- **Картинки файлами на хосте (24.08.2026, v=20260824-41):** загрузка с компьютера идёт через `editorUploadImageFile()` (js/editor.js): сжатие до 1600px (`editorDownscaleImage`, WebP если браузер умеет) → POST `content-sync.php {action:'upload_image'}` → сервер пишет файл `img/u/YYYY-MM/u<sha256-14>.<ext>` (дедуп по хешу контента; тип проверяется getimagesizefromstring; лимит 1,5 МБ; throttleWindow 8/30с) → в правки сохраняется URL. Фолбэк при file://оффлайне — прежний base64. Интеграции: `openImageEditor` + `editorSetupUploadField` (формы портфолио/услуг). Старые base64-правки мигрированы в img/u/.
- **MI_SNAPSHOT (24.08.2026):** после каждой мутации хранилища `refreshSnapshot()` атомарно (tmp+rename, flock `.index-lock`) обновляет ДВА блока в index.html: `<!--MI_SNAPSHOT-->` в head (JSON правок + скрипт, вешающий класс `mi-preload` на `<html>` и CSS `visibility:hidden` для картинок из снимка) и `<!--MI_APPLY-->` перед `</body>` (инлайн-подмена src/текстов из снимка + снятие класса). Итог: посетитель НИКОГДА не видит старую картинку — даже на холодном кэше и до загрузки внешних JS. Фронт: `editorMergeSnapshot()` применяет снимок при загрузке, если synced_at не старше LS-кэша (`<prefix>synced_at`). Публикация = «Сохранить» (деплой кода — отдельно). Пустые map-ключи в снимке кодируются `{}` (не `[]`).
- **Раннее применение:** `editorApplySaved()` вызывается синхронно в конце body (до первой отрисовки — нет «мигания» старых картинок/текстов) и повторно в конце DOMContentLoaded в main.js (для элементов, отрисованных JS: телефоны хедера/футера).
- **Тихий кэш у посетителей:** `editorPersistSilent()` пишет стейт в localStorage без отправки на сервер (вызывается из `editorApplyServerData` после pull/изменения времени) — повторные визиты рендерятся правильно до первого кадра.
- **Чистка картинок:** кнопка «🧹 Чистка» в панели → GET `?action=images_inventory&key=` (файлы + флаги current/history) → POST `{action:'cleanup_unused', key}` удаляет только файлы, на которые не ссылается ни текущее состояние, ни хоть одна версия истории. При откате версии сервер (`resolveMissingImages`) подставляет ближайшую живую версию файла из поздних снапшотов или убирает правку (дефолт).
- **История правок в VK (24.08.2026):** после каждого сохранения/отката/чистки `content-sync.php` сравнивает старое↔новое хранилище (`diffStores`) и шлёт готовый текст на тот же GAS-вебхук (`CHANGE_WEBHOOK_URL`, тип `site_changes`, таймаут 5с, ошибка не мешает сохранению). В ответе сохранения `changes_sent:N`. Правила: добавления/удаления поимённо, точечные правки до 12 строк (дальше «…и ещё N»), пустой дифф не отправляется; особые сообщения «📦 Первичное наполнение», «🗑 Сброс», «↩️ Откат к версии от …», «🧹 Чистка картинок». Словарь меток текстов/картинок — `textLabel()`/`imageLabel()` в content-sync.php. GAS-ветка приёма — в webhook/vk-webhook.gs (`data.type==='site_changes'`, токен по-прежнему только там). Отладочный нюанс: прокси хостинга иногда отдаёт 502 на первый POST после паузы — ретрай 2-3 раза.
- **ИНЦИДЕНТ-КОДИРОВКА (24.08.2026) — НЕ ПОВТОРЯТЬ:** при работе со store через скрипты на этой машине: (1) НИКОГДА `subprocess(..., text=True)` для ответов сервера — Windows-локаль cp1251 молча портит UTF-8; только bytes + `.decode('utf-8')`. (2) Пейлоады POST собирать строго `json.dumps(..., ensure_ascii=True)` — чистый ASCII неубиваем никакой кодировкой. (3) Каждый push должен включать ВСЕ ключи хранилища (editable_images терялись дважды из-за частичных пушей). Серверный предохранитель `payloadHasMojibake()` (content-sync.php): раундрип CP1251→UTF-8 с проверкой русских слов ≥12 букв; мусор отклоняется 422 «encoding looks broken».
- Менеджеры акций/услуг/портфолио по умолчанию показывают исходные данные (не пустоту) и сохраняют весь список при первом изменении.
- Точечные правки структуры услуг: `data-ed-path="svc.<id>.<path>"` (title, shortDesc, price.<i>.section / items.<j>.name / meta / price, masters.<k>.name / desc). Хранятся в `EDITOR_STORE.paths` (localStorage `miedit_service_paths`), применяются в `getServices()` через `applyServicePaths`/`applyDeepPath`. Клик по тексту в попапе/карточке/прайс-аккордеоне открывает редактор. Путь с одним сегментом-массивом (items/masters/images/price) как лист блокируется.
- Единые телефоны `site_phone_0/1` во всех местах (хедер, мобильное меню, футер, контакты): правка одного обновляет `tel:` во всех ссылках через `editorUpdateTelHrefs`.
- Полный конструктор услуги («Добавить услугу» / «Редактировать»): мастера и прайс-разделы/позиции. Функции: `serviceFormHtml`, `renderMastersBox`/`renderPriceBox` (только рендер), `parseMasters`/`parsePrice` (сбор с фоллбэком на прежние данные), `wireServiceForm` (делегированный клик: добавить/удалить мастера, раздел, позицию). При добавлении новой услуги прайс и мастера из формы сохраняются. Наружу: `EDITOR_SECTIONS.services.openForm(existing)`.
- Кеш-версия в HTML: `?v=20260824-41` (js/main|storage|editor), css/editor.css `v=20260824-40`, sections/portfolio+services `v=20260824-38`. Вход в админку: `?edit=КЛЮЧ` или голый `?КЛЮЧ` (слэш в конце адреса не важен). Smoke-тест `C:\Users\user\AppData\Local\Temp\opencode\editor_test.html` (65 проверок): геттеры, тексты/изображения, rerender, реестр изображений, оверрайды, менеджеры, paths (данные+DOM), телефоны (текст+tel:), конструктор (шаблон, добавление строк, сохранение), история (модалка, офлайн-null), editorModeActive (?edit=, голый ключ, ?x=, пусто, неверный ключ). Запуск: Edge headless по `file://` (скрипты в харнессе подключены по file:// — через HTTP не запускать).
- Код: `js/editor.js` (ядро), `js/storage.js` (localStorage/JSON/экспорт), `js/sections/{promotions,services,portfolio}.js`, `css/editor.css`. Разметка правок — атрибуты `data-editable` в HTML страниц (на index 27 элементов: hero, секции, контакты; hero-изображения `hero_img_{back,mid,front}` редактируются). Логотип `hero__brand` (разметка со `<span>`) — не редактируется намеренно.

## Стек
- Чистый HTML/CSS/JS (без фреймворков, без сборщиков).
- Шрифт: Onest (Google Fonts), 300–700.
- Сетки: Flexbox. Брейкпоинт мобильной адаптации: 768px.
- Админка: PHP-синхронизация на хостинге (content-sync.php + JSON-файл). PocketBase как серверный слой — отменён (shared-хостинг, избыточен).
- Обратная связь v1.0: вебхук → Telegram + email-дубль.

## Структура
```
MiStudio/
├── index.html           — Лендинг (все секции: акции, услуги, о нас, портфолио, контакты)
├── config.js            — Конфигурация (URL, телефоны, адрес)
├── content-sync.php     — PHP-синхронизация правок админки (mistudio-data/editor-store.json, вне веб-корня)
├── css/style.css        — Единый файл стилей
├── css/editor.css       — Стили панели и модалок редактора
├── js/main.js           — Единый файл скриптов
├── js/data.js           — Локальный контент (5 категорий, прайс)
├── js/storage.js        — localStorage/JSON/импорт-экспорт для редактора
├── js/editor.js         — Ядро визуального редактора (?edit=FZF6MGgNAq4mU653tHkPvCSP)
├── js/sections/         — Менеджеры секций редактора (promotions, services, portfolio)
└── img/                 — Изображения
```

## Дизайн-токены
- Фон: #FAF9F6 (off-white)
- Заголовки/текст: #1A1A1A (глубокий чёрный)
- Акцент: #B8975A (золото/бронза)
- border-radius: 12px; тени лёгкие
- Кнопки: accent (золотая заливка), outline (чёрная рамка)
- Интерактив: transition 0.3s
- Контейнер: max-width 1200px, padding 0 20px

## Архитектурные решения (задел на v2.0)
1. `submitLead(formData)` — единая точка отправки заявок в `js/main.js`.
   - v1.0: POST на Telegram-вебхук (URL в `config.js`).
   - v2.0: тот же вызов пойдёт на `/api/v2/leads` (FastAPI) — фронт не переписывается.
2. Поле `visit_time` обязательно в форме уже в v1.0.
3. JSON заявки совместим с будущей БД:
   `{ name, phone, service, visit_time, comment, source: "website", version: "1.0" }`
4. PocketBase коллекция `leads` — статусы: v1.0 `new`, в v2.0 добавятся `processing`, `pending_confirmation`, `confirmed`, `cancelled`.
5. URL-структура API: `/api/v1/leads` (сейчас), `/api/v2/leads` (потом).
6. Контент услуг лежит в `js/data.js`. В v2.0 подтягивается с PocketBase через REST (fetch), кэш в localStorage. Переключение источника — через `CONFIG.pocketbaseUrl` в `config.js`, рендер тот же.
   - Уточнение (16.08.2026): вместо PocketBase на хостинге используется лёгкая PHP-синхронизация правок админки (`content-sync.php`); PocketBase остаётся опцией v2.0, но по текущему стеку не требуется.

## Обратная связь: VK-бот сообщества + Email (с 16.08.2026 — основной канал)
- Группа-бот: Studiomi.bot (club240886388). Токен с правом messages хранится ТОЛЬКО в `webhook/vk-webhook.gs` (не в репозитории/не в config.js).
- Беседа для заявок: `VK_PEER_ID = 2000000001` (владелец сайта — админ, участников добавляет сам через VK, скрипт не трогаем).
- `webhook/vk-webhook.gs`: POST от `submitLead()` → `messages.send` в беседу + email-дубль на `studiomi588@gmail.com`. GET `<url>?test` — пробная отправка.
- Важно: бот-группа шлёт ТОЛЬКО в беседы, где она состоит (ошибка 917 — бот не участник/выгнан). Callback API не нужен — Long Poll.
- Статус: ЗАДЕПЛОЕН на корпоративный Google-аккаунт студии (16.08.2026). URL вписан в `CONFIG.leadWebhookUrl` в config.js. Проверено тестовой заявкой: сообщение в беседу + email — ок.
- Telegram-вебхук (`webhook/telegram-webhook.gs`) остался как задел/резерв — не используется.
- PocketBase коллекция `leads` — статусы: v1.0 `new`, в v2.0 добавятся `processing`, `pending_confirmation`, `confirmed`, `cancelled`.

## Android-приложение (v2.1 «полностью нативное», 24.08.2026)
- Папка `android/`. **Сайт НЕ изменяется вообще** — все данные тянет скрытый WebView-загрузчик (`loader`, 1×1px, невидимый): грузит https://studiomi.ru/ один раз и через JS-мост `AppBridge.data(...)` пушит в приложение CONFIG (телефоны/график/адрес/ссылки), getServices() целиком (с прайсом и мастерами), getPromos(), getPortfolioItems(). Обновление данных — иконка ⟳ в тулбаре; при ошибке сети — баннер «Нет сети» сверху. Правки админки подхватываются после обновления.
- ВСЕ экраны нативные (WebView для контента больше нет):
  - **Главная**: hero-карточка (Mi Studio/слоган/график+адрес из CONFIG), «О студии», лента акций из getPromos() (таба «Акции» нет).
  - **Услуги**: карточки категорий (title+shortDesc), тап раскрывает прайс по секциям (name/meta/price) и блок «Мастера» с фото. Ленивое построение тела при первом раскрытии.
  - **Работы**: сетка 2 колонки фото портфолио (MiniImg: http+data:base64, кэш, даунскейл до ~1100px), тап → полноэкранный просмотрщик (AlertDialog на чёрном).
  - **Контакты**: карточки телефон(ы)→tel:, часы работы, адрес→geo:/Яндекс.Карты, кружки VK(#0077FF)/TG(#229ED9) из CONFIG.vkUrl/tgUrl, кнопка онлайн-записи (bookingUrl→dikidiUrl).
- **Таб-бар v2.2.1 — кастомный флекс** (`nav_bar.xml`, styles NavTab/NavTabLabel): LinearLayout из 4 табов (weight=1) + слот `call_slot` 76dp по центру с кнопкой 58dp и PulseView 150dp (clipChildren=false по всей цепочке — кольца свободно выходят за бар). Ничего не налезает на соседние табы. Выделение таба — `setSelected()` + selector nav_item (state_selected).
- **PulseView (v2.2.2)**: волны из 3 колец при старте и затем **раз в 5 секунд** (PERIOD_MS=5000; v2.2.3).
- **Системные бары не перекрывают приложение (v2.2.3)**: `WindowCompat.setDecorFitsSystemWindows(true)` в onCreate + `fitsSystemWindows` на корневом FrameLayout — targetSdk 35 включает edge-to-edge на Android 15+, из-за чего статус-бар налезал на тулбар. Статус-бар окрашен в цвет приложения (#FAF9F6, светлые иконки через windowLightStatusBar в теме).
- **Шторка заявки не закрывается свайпом вниз (v2.2.2)**: BottomSheetBehavior setHideable(false) + колбэк возвращает STATE_EXPANDED при попытке свернуть — иначе жест свайпа конфликтовал со скроллом формы сайта. Закрытие: ✕ в попапе (closeLead) или «Назад».
- **Главная без скролла (v2.2.1)**: LinearLayout вместо ScrollView; фото `home_photo` weight=1 (резиновое, minHeight 100dp) занимает всё свободное место; «О студии» и hint убраны.
- **Главная v2.2**: + фото с сайта (`home_photo`, 190dp) — берётся из hero-картинок страницы (`hero__plane--front/mid/back img` через мост), фолбэк — первое фото портфолио.
- **Форма заявки v2.3 — НАТИВНАЯ копия сайтовой** (WebView-шторка с сайтом удалена, сайт больше нигде не мелькает): `sheet_lead.xml` + showLeadForm(). Состав 1:1 с попапом сайта: Имя*; «Как связаться» сегменты Телефон*(+7, маска)/Почта/Telegram; Услуги* — мультидиалог по категориям getServices(), бейджи с ✕; Акции — бейджи (при тапе из карточки акции предзаполнены); Желаемые даты* — системный DatePickerDialog мультивыбор, бейджи отсортированы; Комментарий; согласие текстом. Payload идентичен сайту: name/contacts(сводка через запятую)/phone(+7…)/phone_display/email/telegram(@)/services[]+service/promos[]+promo/visit_dates[]+visit_dates_display/comment/_hp/_ts/source:'android-app'. Валидация как на сайте (тексты те же). Шторка зафиксирована STATE_EXPANDED (setHideable(false)+колбэк) — свайп не закрывает.
- **Дубли заголовков убраны (v2.3)**: в телах страниц Услуги/Работы/Контакты больше нет своих заголовков — название раздела только в тулбаре.
- **Услуги v2.2**: внутри раскрытой категории сначала блок «Мастера» (фото+имя+описание), затем прайс по секциям.
- Форма записи (кнопка «Записаться») — нативный BottomSheet: имя/телефон/услуга(Spinner из getServices())/время/комментарий → POST на CONFIG.leadWebhookUrl (`source:'android-app'`) → беседа VK+email; вторая кнопка онлайн-запись.
- Ограничение v2.1: раз видимых WebView больше нет, админ-CMS с телефона недоступна (работает на сайте в браузере). Оффлайн: нативные страницы показывают заглушку «Нет сети», ⟳ перезагружает данные.
- Сборка: GitHub Actions `.github/workflows/android-apk.yml`; артефакт всегда, на теге `v*` — Release. Подпись: `android/signing/mistudio.p12` в приватном репо (пароль в app/build.gradle; репо не публиковать).
- **v2.3.1 правки UX**: ✕-крестик на форме заявки (закрывает шторку); описание категорий услуг — полное, свёрнуто до 3 строк, тап раскрывает/сворачивает; у карточек акций кнопка «＋ Добавить в заявку» (золотая = не добавлено, серая «✓ В заявке — убрать» = добавлено; тап переключает). Выбор синхронизирован в обе стороны: бейджи в форме ↔ кнопки на карточках (promosCache + refreshPromosRow/syncLeadPromoBadges). Выбор сохраняется между открытиями формы и сбрасывается после успешной отправки.
- **v2.4 правки UX**:
  - **Синхронизация CMS-данных исправлена**: сайт применяет правки админки асинхронно (`editorSyncPull` → `editorApplyServerData`) уже ПОСЛЕ загрузки страницы — мост читал только базовые data.js (поэтому удалённые на сайте услуги оставались в приложении). Теперь DATA_JS пушит данные сразу + хук на `window.editorApplyServerData` (переопределение с повторным пушем через 50мс) + страховочный таймаут 4с.
  - **Услуги: раздельные зоны клика**. Тап по названию/стрелочке = раскрыть/свернуть содержимое (мастера+прайс). Описание — отдельная зона: полное, свёрнуто до 3 строк; под ним «Читать полностью»/«Свернуть» (видна только если текст реально длиннее 3 строк — детект measure-ом в post()).
  - **Даты: свой мультикалендарь** (`showMultiDatePicker`, без перезапусков): сетка месяца, ‹ › навигация, тапы по дням переключают выделение в любом количестве/месяцах, выбранный день — золотой круг, сегодня — золотой текст+bold. «ОК» применяет к бейджам.
  - **Бейджи**: крестик ✕ прижат к правому краю бейджа независимо от длины текста (LinearLayout label(maxWidth 200dp, ellipsize)+✕).
  - **v2.4.1 фиксы**: стрелка услуг 26sp, шапка baselineAligned=false + minHeight 52dp (стрелка крупная и по центру справа); мультикалендарь переписан со GridLayout (взвешенные ряды схлопывались в 0 — видно было только «Месяц Год») на LinearLayout-строки 7×N.
  - **v2.4.2 фиксы**: «Читать полностью» снова работает — детект числа строк через StaticLayout (старый measure-в-post не работал, пока вкладка Услуг скрыта: ширина GONE-вьюхи = 0). Стрелка услуги вынесена из заголовка в отдельную колонку и центрирована по всей высоте карточки; тапы: название/стрелка = контент, «Читать полностью» = только текст.
- Версии: versionCode/versionName в `android/app/build.gradle`. История: v1.0(1); v1.1(2); v1.1.1(3); v1.1.2(4); v2.0(5); v2.1(6); v2.2(7); v2.2.2(8); v2.2.3(9); v2.3(10); v2.3.1(11); v2.3.2(12); v2.4(13); v2.4.2(16) read-more/стрелка; **v2.4.3(17) — баннер онлайн-записи на главной вместо фото**.
- **Главная v2.4.3**: вместо фото — рабочий баннер «Онлайн-запись» (`booking_banner`, weight=1): 📅 + заголовок + подзаголовок (YClients, если задан CONFIG.bookingUrl, иначе DiKiDi); клик открывает соответствующий сервис. При появлении YClients достаточно вписать bookingUrl на сайте — баннер переключится сам.
- minSdk 24, targetSdk 35, applicationId `ru.studiomi.app`, material 1.12.0, Java 17.

## Не делать в v1.0
FastAPI, SQLite, AI-агенты, планировщик, автоответчик, VK-интеграция, сложная обработка заявок.

## Ссылки
- DiKiDi (онлайн-запись): https://dikidi.net/2049120?p=0
- Референс-донор: Desktop/Mi studio 2/Index.html
- Живой референс: https://mibeauty-studio.ru/
- Контакты: +7 (933) 430-47-77; Н. Новгород, Пятигорская ул., 14
- Соцсети: VK https://vk.ru/club239375190, TG https://t.me/+O-PmXu8y27FkMDg6
